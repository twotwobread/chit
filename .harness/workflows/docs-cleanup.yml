name: docs-cleanup
version: 1

policy: ".harness/policies/default.yml"
rulepack: ".harness/policies/rulepacks/docs-cleanup.yml"

phases:
  docs.cleanup:
    contract: ".harness/contracts/docs-cleanup.contract.md"
    provider: "docs-cleanup-provider"

checks:
  before_complete:
    - stale-reference-search
    - adapter-sync
