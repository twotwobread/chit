name: staging-deploy
version: 1

policy: ".harness/policies/default.yml"
rulepack: ".harness/policies/rulepacks/staging-deploy.yml"

phases:
  staging.deploy:
    contract: ".harness/contracts/staging-deploy.contract.md"
    provider: "staging-deploy-provider"

checks:
  before_mobile_build:
    - mobile-expo-sdk-compatibility
  before_output:
    - secret-log-safety
