# Context Management

- Start with request, linked issue/spec, and narrow code facts.
- Use `rg`/`find` before opening files.
- Stop reading when a source of truth answers the question.
- Expand only for a named blocker.
- Avoid generated/vendor/build artifacts unless they are the target or the source is unavailable.
