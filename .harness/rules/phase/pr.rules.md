# Phase Rules: pr.create

- Create/update PRs only after required evaluation gates pass or accepted risks are explicit.
- Keep PR body concise: scope, verification, gaps, risks.
- Include feature spec and implementation in the same feature PR by default.
- Do not paste long logs or secrets.
