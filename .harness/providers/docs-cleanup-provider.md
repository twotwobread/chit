# Provider: docs-cleanup-provider

## Phase

`docs.cleanup`

## Behavior

Perform targeted docs/rules/skills cleanup using `.harness` as source of truth. Use `rg` for reference inventory, keep `AGENTS.md` small, and avoid reading broad product docs unless directly targeted.
