# Provider: pr-lifecycle-provider

## Phase

`pr.lifecycle`

## Behavior

Use git and GitHub CLI/UI to inspect readiness, create/update PRs, check CI, merge when requested, and clean up branches/worktrees safely.

Do not treat PR lifecycle work as feature implementation.
